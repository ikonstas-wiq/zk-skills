# Python
.venv/
venv/
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Node
node_modules/

# Personal, interchangeable voice profile — populated files live locally only.
# The committed templates live in write-guide/examples/; the real profile
# (SKILL.md at the root, AUDIENCE.md in references/) is never committed.
/write-guide/SKILL.md
/write-guide/references/

# Personal Slack conversation map — names and channel IDs; never commit.
# The committed template lives at slack/references/metadata_map.example.json
/slack/references/metadata_map.json

# Personal, unshared skill — kept local only.
/friday-reply/
